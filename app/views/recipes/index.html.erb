<ul class='lists-right'>
  <% if user_signed_in? %>
    <li><%= link_to current_user.nickname, "#", class: "user-nickname" %></li> 
    <li><%= link_to 'ログアウト', destroy_user_session_path, method: :delete, class: "logout" %></li> 
  <% else %>
    <li><%= link_to 'ログイン', new_user_session_path, class: "login" %></li>
    <li><%= link_to '新規登録', new_user_registration_path, class: "sign-up" %></li>
  <% end %>
</ul>

<h1>トップページ</h1>

<% @recipes.each do |recipe| %>
  <div>投稿者：<%= recipe.user.nickname %></div>
  <div>料理の名前：<%= link_to recipe.name, "#" %></div>
  <div><%= link_to image_tag(recipe.image), "#" %></div>
  <div>料理の説明：<%= recipe.caption %></div>
  <div>調理時間：<%= recipe.cooking_time %>分</div><br>
<% end %>

<br><br><%= link_to '投稿する', new_recipe_path, class: "recipe-new" %>